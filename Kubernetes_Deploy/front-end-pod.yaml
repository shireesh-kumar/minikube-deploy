apiVersion: v1
kind : Pod
metadata:
  name: front-end-pod
  labels:
    name: front-end-pod
    app: demo-voting-app
  
spec:
  containers:
    - name : node
      image : shireesh1998/node:latest
      ports:
        - containerPort: 3000